- name: build
  service: app
  command: yarn

- name: list build files
  service: app
  command: ls -la

# - name: list files
#   service: awsdeployment
#   command: ls -la

# - name: deploy
#   service: awsdeployment
#   command: aws s3 sync /app/public s3://static-deploy-test
